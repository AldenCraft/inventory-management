# GitHub Personal Access Token used by the `github` MCP server in .mcp.json.
# Create one at https://github.com/settings/tokens (a fine-grained or classic PAT
# with read access is enough for the demo). Copy this file to .env and fill it in.
GITHUB_PERSONAL_ACCESS_TOKEN=
